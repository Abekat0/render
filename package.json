{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "\"echo \\\"Error: no test specified\\\" && exit 1\"",
    "build:ui": "rm -rf dist && cd ../FullStack/part3/3b  && npm run build && cp -r dist ../../../render",
    "deploy:ui": "npm run build:ui && git add . && git commit -m uibuild && git push origin HEAD:main"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "backend": "file:",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.5"
  }
}
